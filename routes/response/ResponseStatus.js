'use strict';

module.exports = Object.freeze({
    OK: Number(200), 
    CREATED: Number(201), 
    ERROR: Number(400), 
    NOT_FOUND: Number(404), 
    INTERNAL_ERROR: Number(500),
    UNATHORIZED: Number(401)
});

